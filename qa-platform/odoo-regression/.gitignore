.env
.env.local
config/local.yaml
*.pid
wip/
venv/
__pycache__/
*.pyc
artifacts/
# anchored: only the runtime data dir at the repo root (results.db,
# baselines, test_registry.json). An unanchored 'data/' also matched
# reports/data/, which silently excluded every fgXX_feasibility.json.
/data/
.pytest_cache/

# docker build transcript produced by `docker compose build > build.log`
build.log
# a natively-run instance's own Odoo home (conf, filestore, logs) --
# 21k files of runtime state, never source
instances/
